# Virtual Environment Setup

## ✅ Virtual Environment Created Successfully!

A virtual environment has been created for this project at: `venv/`

## 📋 Requirements File

All dependencies are listed in `requirements.txt`:
- pandas, numpy (data processing)
- matplotlib, seaborn (visualization)
- jupyter, jupyterlab (notebook support)
- requests, Pillow, python-dotenv (API and utilities)
- openai (OpenAI API support)

## 🚀 How to Use the Virtual Environment

### Activate the virtual environment:
```bash
source venv/bin/activate
```

### Deactivate when done:
```bash
deactivate
```

### Run Jupyter Notebook:
```bash
# After activating venv
jupyter notebook
# or
jupyter lab
```

### Run the annotation analysis notebook:
```bash
# After activating venv
jupyter notebook annotation_analysis.ipynb
```

### Install additional packages:
```bash
# After activating venv
pip install package_name
pip freeze > requirements.txt  # Update requirements file
```

## 📊 Quick Start

1. Activate virtual environment:
   ```bash
   source venv/bin/activate
   ```

2. Launch Jupyter:
   ```bash
   jupyter notebook
   ```

3. Open `annotation_analysis.ipynb` in Jupyter

4. Run all cells to see the analysis and plots!

## ✅ Verification

All packages are installed and verified:
- ✅ Python 3.12.3
- ✅ Jupyter & JupyterLab
- ✅ pandas, numpy
- ✅ matplotlib, seaborn
- ✅ All other dependencies

